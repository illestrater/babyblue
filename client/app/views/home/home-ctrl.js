import _ from 'lodash';

/* @ngInject */
function HomeCtrl($rootScope, $scope, $state, $timeout, $log, AppSettings) {
    const vm = this;
}

export default {
    name: 'HomeCtrl',
    fn:   HomeCtrl
};
